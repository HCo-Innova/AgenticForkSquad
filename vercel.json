{
  "buildCommand": "cd frontend && npm ci --force && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm ci --force",
  "nodeVersion": "20.10.0",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
