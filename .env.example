# =================================================================
# == AFS (Agentic Fork Squad) Project - Environment Variables
# =================================================================
#
# This file provides an example of the environment variables
# needed to run the application locally and in production.
#
# Instructions:
# 1. Copy this file to .env
# 2. Fill in the actual values for your environment
# 3. NEVER commit .env to version control
# 4. NEVER share credentials via email/chat/docs
#
# =================================================================

# ----------------------------------
# PostgreSQL (Development Local)
# ----------------------------------
# Only used when USE_TIGER_CLOUD=false (local development)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=afs_dev
POSTGRES_USER=afs_user
POSTGRES_PASSWORD=change_me_secure_password

# ----------------------------------
# Redis Cache
# ----------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=change_me_redis_password
REDIS_DB=0
REDIS_URL=redis://:change_me_redis_password@redis:6379/0

# ----------------------------------
# Backend Server Configuration
# ----------------------------------
PORT=8000
ENV=development
LOG_LEVEL=debug
RUN_MIGRATIONS=true

# ----------------------------------
# Tiger Cloud Database (Production)
# ----------------------------------
# Contact Tiger Cloud support for your project credentials.
# DO NOT hardcode these; use secure credential management.
USE_TIGER_CLOUD=true
TIGER_PROJECT_ID=your_project_id_here
TIGER_MAIN_SERVICE=your_main_service_id_here
TIGER_SERVICE_ID=your_service_id_here
TIGER_DB_USER=your_db_user_here
TIGER_DB_PASSWORD=your_db_password_here
TIGER_DB_HOST=your_service_id.your_project_id.tsdb.cloud.timescale.com
TIGER_DB_PORT=5432
TIGER_DB_NAME=tsdb
TIGER_DB_SSLMODE=require
DATABASE_URL=postgres://user:password@host:port/dbname?sslmode=require

# Tiger fork services for agents (pre-created in Tiger Cloud)
TIGER_FORK_A1_SERVICE_ID=your_fork_a1_service_id_here
TIGER_FORK_A1_PASSWORD=your_fork_a1_password_here
TIGER_FORK_A1_HOST=your_fork_a1_host_here
TIGER_FORK_A1_PORT=5432

TIGER_FORK_A2_SERVICE_ID=your_fork_a2_service_id_here
TIGER_FORK_A2_PASSWORD=your_fork_a2_password_here
TIGER_FORK_A2_HOST=your_fork_a2_host_here
TIGER_FORK_A2_PORT=5432

# Tiger API credentials (for MCP access)
TIGER_PUBLIC_KEY=your_public_key_here
TIGER_SECRET_KEY=your_secret_key_here
TIGER_MCP_URL=http://mcp:9090
TIGER_MCP_HOST=0.0.0.0
TIGER_MCP_PORT=9090

# ----------------------------------
# Google Cloud & Vertex AI (Gemini Models)
# ----------------------------------
# Path to GCP service account JSON file (Application Default Credentials)
# See: https://cloud.google.com/docs/authentication
GOOGLE_APPLICATION_CREDENTIALS=./secrets/gcp_credentials.json
VERTEX_PROJECT_ID=your_gcp_project_id_here
VERTEX_LOCATION=us-central1

# Gemini model selection per agent role
GEMINI_CEREBRO_MODEL=gemini-2.5-pro
GEMINI_OPERATIVO_MODEL=gemini-2.5-flash
GEMINI_BULK_MODEL=gemini-2.0-flash

# ----------------------------------
# Frontend (Vite + React)
# ----------------------------------
# URLs used by the frontend to connect to the backend API and WebSocket
# For local development: use relative paths or localhost
# For production: use full URLs or rely on reverse proxy
VITE_API_URL=http://localhost/api
VITE_WS_URL=ws://localhost/ws
NODE_ENV=development
